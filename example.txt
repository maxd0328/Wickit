
START #expr;

#expr -> #expr $PLUS #term;
#expr -> #expr $MINUS #term;
#expr -> #term;

#term -> #term $TIMES #factor;
#term -> #term $DIVBY #factor;
#term -> #factor;

#factor -> $LPAREN #expr $RPAREN;
#factor -> $NUMBER;
#factor -> $NUMBER $PLUS;
